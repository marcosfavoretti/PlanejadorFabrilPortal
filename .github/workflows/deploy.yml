name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: marcosfavoretti/planejadorfabrilportal

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      should_build: ${{ steps.impact.outputs.should_build }}
      should_deploy: ${{ steps.impact.outputs.should_deploy }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect deploy impact
        id: impact
        run: bash ./scripts/ci/detect-deploy-impact.sh "${{ github.event.before }}" "${{ github.sha }}"

  validate:
    needs: prepare
    if: ${{ needs.prepare.outputs.should_build == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run security audit
        run: npm run security:audit

      - name: Run GitLeaks
        run: npm run security:gitleaks

      - name: Run Semgrep
        run: npm run security:semgrep

      - name: Validate committed API client
        run: npm run check:api-client

      - name: Build Angular Application
        run: npm run build

  build-and-push:
    needs: [prepare, validate]
    if: ${{ needs.prepare.outputs.should_build == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,format=long

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  smoke-test:
    if: ${{ needs.prepare.outputs.should_build == 'true' }}
    needs: [prepare, build-and-push]
    runs-on: ubuntu-latest
    steps:
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull and Run image for testing
        run: |
          IMAGE_URL=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
          docker pull $IMAGE_URL
          
          # Remove o container se ele já existir (de builds falhos anteriores)
          docker rm -f smoke-test-app || true
          
          docker run -d --name smoke-test-app -p 8086:8085 $IMAGE_URL
          
          echo "Aguardando inicialização do Nginx..."
          cleanup() {
            docker stop smoke-test-app >/dev/null 2>&1 || true
            docker rm smoke-test-app >/dev/null 2>&1 || true
          }

          trap cleanup EXIT

          for attempt in $(seq 1 15); do
            if curl -fsS http://localhost:8086/index.html > /dev/null; then
              echo "SUCCESS: Frontend acessível e retornando index.html"
              exit 0
            fi

            if [ "$(docker inspect -f '{{.State.Running}}' smoke-test-app 2>/dev/null)" != "true" ]; then
              echo "ERROR: Container encerrou antes de responder ao smoke test"
              docker logs smoke-test-app
              exit 1
            fi

            sleep 2
          done

          echo "ERROR: Falha ao acessar o frontend após o build"
          docker logs smoke-test-app
          exit 1

  deploy:
    if: ${{ needs.prepare.outputs.should_deploy == 'true' }}
    needs: [prepare, smoke-test]
    runs-on: [self-hosted, Linux, X64, frontend]
    permissions:
      contents: read
      packages: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy to Swarm
        run: |
          export IMAGE_TAG=sha-${{ github.sha }}
          IMAGE_URL=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${IMAGE_TAG}
          echo "Deploying image: $IMAGE_URL"
          docker stack deploy -c deploy/stack-front.yml ethosfront --with-registry-auth --resolve-image always
