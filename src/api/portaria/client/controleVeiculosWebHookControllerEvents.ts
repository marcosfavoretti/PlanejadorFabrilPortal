/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/client';
import type { ControleVeiculosWebHookControllerEventsQueryResponse } from '../models/ControleVeiculosWebHookControllerEvents';
import type { Client, RequestConfig, ResponseErrorConfig } from '@/client';

function getControleVeiculosWebHookControllerEventsUrl() {
  const res = {
    method: 'GET',
    url: `https://app.ethos.ind.br/api/controle-portaria/veiculos/webhook/events` as const,
  };
  return res;
}

/**
 * @summary Stream de eventos de veículos em tempo real (SSE)
 * {@link /api/controle-portaria/veiculos/webhook/events}
 */
export async function controleVeiculosWebHookControllerEvents(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ControleVeiculosWebHookControllerEventsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: getControleVeiculosWebHookControllerEventsUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
