{
  "type": "object",
  "properties": {
    "total": { "type": "number" },
    "page": { "type": "number" },
    "limit": { "type": "number" },
    "totalPages": { "type": "number" },
    "data": {
      "description": "Lista de itens paginados do tipo VehicleResponseDto.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "placa": { "type": "string" },
          "marca": { "type": "string" },
          "modelo": { "type": "string" },
          "ano": { "type": "number" },
          "cor": { "type": "string" },
          "createdAt": { "format": "date-time", "type": "string" },
          "updatedAt": { "format": "date-time", "type": "string" },
          "status": {
            "type": "string",
            "enum": ["INSIDE", "OUTSIDE", "FORCE_CLOSE"]
          },
          "fotoUrl": { "type": "string" }
        },
        "required": [
          "id",
          "placa",
          "marca",
          "modelo",
          "ano",
          "createdAt",
          "updatedAt",
          "status"
        ],
        "x-readme-ref-name": "VehicleResponseDto"
      }
    }
  },
  "required": ["total", "page", "limit", "totalPages", "data"],
  "x-readme-ref-name": "PaginatedVehicleResponseDtoDto"
}
