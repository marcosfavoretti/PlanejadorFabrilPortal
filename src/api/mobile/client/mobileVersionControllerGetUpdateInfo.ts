/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/client';
import type { MobileVersionControllerGetUpdateInfoQueryResponse } from '../models/MobileVersionControllerGetUpdateInfo';
import type { Client, RequestConfig, ResponseErrorConfig } from '@/client';

function getMobileVersionControllerGetUpdateInfoUrl() {
  const res = {
    method: 'GET',
    url: `https://app.ethos.ind.br/api/mobile/version/update` as const,
  };
  return res;
}

/**
 * @description Retorna a versão, o código da versão, a URL do APK, a obrigatoriedade da atualização e o changelog da última versão registrada.
 * @summary Busca informações da atualização disponível do app
 * {@link /api/mobile/version/update}
 */
export async function mobileVersionControllerGetUpdateInfo(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    MobileVersionControllerGetUpdateInfoQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: getMobileVersionControllerGetUpdateInfoUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
