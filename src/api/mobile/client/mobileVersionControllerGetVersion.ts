/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/client';
import type { MobileVersionControllerGetVersionQueryResponse } from '../models/MobileVersionControllerGetVersion';
import type { Client, RequestConfig, ResponseErrorConfig } from '@/client';

function getMobileVersionControllerGetVersionUrl() {
  const res = {
    method: 'GET',
    url: `https://app.ethos.ind.br/api/mobile/version` as const,
  };
  return res;
}

/**
 * @description Retorna a versão mais recente registrada para o aplicativo mobile.
 * @summary Busca a versão atual do app React Native
 * {@link /api/mobile/version}
 */
export async function mobileVersionControllerGetVersion(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    MobileVersionControllerGetVersionQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: getMobileVersionControllerGetVersionUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
