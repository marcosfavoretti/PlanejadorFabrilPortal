/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/client';
import type { AuthControllerListUsersQueryResponse } from '../models/AuthControllerListUsers';
import type { Client, RequestConfig, ResponseErrorConfig } from '@/client';

function getAuthControllerListUsersUrl() {
  const res = {
    method: 'GET',
    url: `https://app.ethos.ind.br/api/auth/users` as const,
  };
  return res;
}

/**
 * @summary List all users
 * {@link /api/auth/users}
 */
export async function authControllerListUsers(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    AuthControllerListUsersQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: getAuthControllerListUsersUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
