{
  "type": "object",
  "properties": {
    "moeda": { "type": "string", "example": "BRL" },
    "view": {
      "type": "string",
      "enum": ["ano", "mes", "dia"],
      "example": "mes"
    },
    "dataInicio": { "type": "string", "example": "2026-08-20" },
    "dataFim": { "type": "string", "example": "2026-09-18" },
    "custoTotal": {
      "type": "number",
      "example": 19342.1,
      "description": "Custo total do período em reais."
    },
    "custoTotalFormatado": { "type": "string", "example": "R$ 19.342,10" },
    "periodos": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "data": {
            "type": "string",
            "example": "2026-09-18",
            "description": "YYYY-MM para view=ano; YYYY-MM-DD para view=mes ou view=dia."
          },
          "custo": {
            "type": "number",
            "example": 1248.5,
            "description": "Custo do dia em reais."
          },
          "custoFormatado": { "type": "string", "example": "R$ 1.248,50" },
          "quantidadeFolhas": { "type": "number", "example": 3 }
        },
        "required": ["data", "custo", "custoFormatado", "quantidadeFolhas"],
        "x-readme-ref-name": "KpiCustoFolhaHoraExtraPeriodoDTO"
      }
    }
  },
  "required": [
    "moeda",
    "view",
    "dataInicio",
    "dataFim",
    "custoTotal",
    "custoTotalFormatado",
    "periodos"
  ],
  "x-readme-ref-name": "ResKpiCustoFolhaHoraExtraDTO"
}
