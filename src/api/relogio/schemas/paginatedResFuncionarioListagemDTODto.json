{
  "type": "object",
  "properties": {
    "total": { "type": "number" },
    "page": { "type": "number" },
    "limit": { "type": "number" },
    "totalPages": { "type": "number" },
    "data": {
      "description": "Lista de itens paginados do tipo ResFuncionarioListagemDTO.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "matricula": { "type": "string" },
          "nome": { "type": "string" },
          "setor": { "type": "string" },
          "turnoBase": {
            "type": "string",
            "description": "Código do turno base cadastrado para o funcionário",
            "example": "001"
          },
          "temporario": {
            "type": "boolean",
            "description": "Indica se o funcionario veio da planilha de temporarios",
            "example": false
          }
        },
        "required": ["matricula", "nome", "setor", "turnoBase", "temporario"],
        "x-readme-ref-name": "ResFuncionarioListagemDTO"
      }
    }
  },
  "required": ["total", "page", "limit", "totalPages", "data"],
  "x-readme-ref-name": "PaginatedResFuncionarioListagemDTODto"
}
